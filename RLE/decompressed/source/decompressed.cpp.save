#include "decomressed.h"

decomp_file(FILE *fp, FILE *c_fp)
{
    int symbol = 0;
    int count  = 0;
    while (count != EOF)
    {
        count = gets(c_fp);
        for ( int i = 0; i < count; ++i)
            


